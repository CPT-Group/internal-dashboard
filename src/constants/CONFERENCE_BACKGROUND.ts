/**
 * Conference room background slideshow – images under public/background/background-conf-room/
 * List is generated at build/dev by scripts/generate-conference-slides.js from that folder.
 * Add/remove images in the folder; run `npm run dev` or `npm run build` so the script updates the list (do not run `next dev` directly or the list can be stale).
 */
export { CONFERENCE_BACKGROUND_SLIDES } from './conferenceBackgroundSlides.generated';
